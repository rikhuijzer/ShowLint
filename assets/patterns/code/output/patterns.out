
~~~ 
<h3 id="1">1: Replace Array{T,1} with Vector{T}</h3>
~~~
tags: *julia*

Vector{T} and AbstractVector{T} are aliases for respectively Array{T,1} and AbstractArray{T,1}.
~~~
<pre><code class="plaintext">match='Array{:[T],1}'

rewrite='Vector{:[T]}'
</code></pre>
~~~


~~~ 
<h3 id="2">2: Use ismissing</h3>
~~~
tags: *julia*

Note that `ismissing` can be slower, see <https://github.com/JuliaLang/julia/issues/27681>.
~~~
<pre><code class="plaintext">match=':[[var]] :[first~[=|!]]== missing' 

rule='where rewrite :[first] { "=" -> "" }'

rewrite=':[first]ismissing(:[var])'
</code></pre>
~~~


~~~ 
<h3 id="3">3: Avoid x -> f(x)</h3>
~~~
tags: *julia*

From the [Julia Style Guide](https://docs.julialang.org/en/v1/manual/style-guide/).
~~~
<pre><code class="plaintext">match=':[[x]] -> :[f~[\w_]*](:[x]):[end~(,|\n)]'

rewrite=':[f]:[end]'
</code></pre>
~~~


~~~ 
<h3 id="4">4: Omit comparison with boolean constant</h3>
~~~
tags: *generic*

From [staticcheck](https://staticcheck.io/docs/checks).
~~~
<pre><code class="plaintext">match=':[[var]] :[first~(=|!)]= :[bool~(true|false)]'

rule='where 
    rewrite :[first] { "=" -> "" },
    rewrite :[bool] { "false" -> "!" },
    rewrite :[bool] { "true" -> "" }'

rewrite=':[first]:[bool]:[var]'
</code></pre>
~~~


~~~ 
<h3 id="5">5: Omit a == a and a != a</h3>
~~~
tags: *julia*

SA4000 in [staticcheck](https://staticcheck.io/docs/checks).
~~~
<pre><code class="plaintext">match=':[x~[\w_\[\]:\.()]*] :[bool~(=|!)]= :[x~[\w_\[\]:\.()]*]'

rule='where
    rewrite :[bool] { "=" -> "true" },
    rewrite :[bool] { "!" -> "false" }'

rewrite=':[bool]'
</code></pre>
~~~


~~~ 
<h3 id="6">6: Avoid findfirst in conditional</h3>
~~~
tags: *julia*

For example, instead of `findfirst('a', "ab") === nothing`,
use `occursin('a', "ab")` or `contains` (Julia ≥1.5).

~~~
<pre><code class="plaintext">match='findfirst(:[a], :[b]) :[bool~(=|!)]== nothing'

rule='where
    rewrite :[bool] { "!" -> "" },
    rewrite :[bool] { "=" -> "!" }'

rewrite=':[bool]occursin(:[a], :[b])'
</code></pre>
~~~

